.env
*.env.local

# Build output
/hq
/bin/
frontend/node_modules/
frontend/dist/

# The React bundle is generated; only the placeholder is tracked so `go build`
# works on a fresh clone before the frontend has been built.
internal/web/dist/*
!internal/web/dist/.gitkeep

.DS_Store
